#!/bin/bash
# Refresh eww widgets after theme change.
# Close then reopen to avoid stacking duplicate windows.
# eww reload re-runs all polls so colors update immediately.
eww close-all 2>/dev/null || true
eww daemon 2>/dev/null || true
eww reload
eww open ticktick
eww open clock
